program gcd;

{
    integer x = 14;
    integer y = 56;
    while x <> y do {
        If x > y then {
            x = x - y;
        } else {
            y = y - x;
        };
    };
    out("GCD: ", x);
}
