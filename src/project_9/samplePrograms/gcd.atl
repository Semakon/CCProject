program gcd;

{
    in("x? ", x);
    in("y? ", x);
    while x <> y do {
        If x > y then {
            x = x - y;
        } else {
            y = y - x;
        };
    };
    out("GCD: ", x);
}
